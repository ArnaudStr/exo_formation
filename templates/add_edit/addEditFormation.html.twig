{% extends 'base.html.twig' %}

{% block body %}
   {{ form_start(form) }}


    {{form_row(form.nom) }} 
    {{form_row(form.nbPlaces) }}
    {{form_row(form.dateDebut) }}
    {{form_row(form.dateFin) }}

    {# <input type='hidden' id="maxNb" value = {{  }}> #}

    {# <ul class="collections" data-prototype= "{{ form_row(form.modules.vars.prototype)|e('html_attr') }}">

        {% for module in form.modules %}
            <li>
                {{ form_row(module) }}
            </li>
            <li>
                {{ form_row(form.duree) }}
            </li>

        {% endfor %} #}

    {# </ul> #}

    {# {% if editMode %}
        <ul class="collections" data-prototype= "{{ form_row(form.stagiaires.vars.prototype)|e('html_attr') }}">

            {% for stagiaire in form.stagiaires %}
                <li>
                    {{ form_row(stagiaire) }}
                </li>
            {% endfor %}

        </ul>
    {% endif %} #}

    {# {% form_theme form 'add_edit/jquery.collection.html.twig' %}
    {{ form(form) }} #}
    

      {{form_row(form.submit) }} 
{{ form_end(form) }} 

{% endblock %}

{% block javascripts %}
    {# <script src="{{ asset('js/jquery.js') }}"></script>
    <script src="{{ asset('js/jquery.collection.js') }}"></script>

    <script type="text/javascript">
        $('.my-selector').collection();
    </script> #}

    {{ parent()}}
    <script src="/js/collectionHandler.js"></script>
{% endblock %}